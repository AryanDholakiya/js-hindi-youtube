"use strict"; //treat all js code as a newer version

console.log(3 + 3);
console.log("ABC");


//data types examples:

let name = "HELLO"
let age = 20
let isloggedIn = true
let state; //example of undefined //value is not assigned
let district = null //representation of empty value //standalone value

// datatypes 
// number 
// bigint
// string => we can use  " "  or ' ' ....prefer " "
// boolean => true or false
// null => null is a type or it is a stand alone value //representation of empty value
// undefined => when we only declared a value but didn't give it value(didn't initialized)
// symbol => unique
// object

console.log(typeof "abcd");
console.log("the typeof of null: " + typeof null); //object
console.log("typeof of undefined: " + typeof undefined); //undefined